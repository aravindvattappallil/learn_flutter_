/*import 'package:flutter/material.dart';
import 'package:flutter_app/widget/listwidgetu.dart';

class Newcard extends StatelessWidget {
  final data datas;
  Newcard({Key key, this.datas}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      child: Column(
        children: [
          Container(
            color: Colors.redAccent,
            child: Column(
              children: [
                Expanded(
                    child: Icon(
                  datas.icon,
                  size: 50,
                )),
                Text(
                  datas.title,
                  style: TextStyle(fontSize: 20),
                )
              ],
            ),
          ),
        ],
      ),
    );
  }
}
*/
